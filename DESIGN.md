# 电影节排片工作台

## 方向

界面像一份安静的电影节随身片单：轻、密、清楚。用户打开后应该先看到片名、时间和状态，而不是看到一堆按钮和色块。避免营销页、说明页和装饰性卡片，优先服务“筛片、选场、确认冲突”。

## 视觉

- 背景用微暖白，正文用近黑，边线用低对比灰。
- 主强调色只小面积出现；不要大面积红、绿、蓝色块。
- 绿色表示已加入排片，琥珀表示冲突或待处理，但都以细线、浅底、小标签表达。
- 圆角保持 8rpx，边线轻，不用阴影。
- 字号克制：标题清楚，列表内容紧凑，英文和元信息弱化。

## 组件

- 想看程度用低饱和度分段状态条，不做厚按钮。
- 选场、移除这类动作固定宽度，视觉上像轻量指令。
- 场次页以时间列为主轴，电影信息为主体，选场动作靠右。
- 排片页以日期分组，冲突只在有冲突时出现。

## 文案

页面只保留必要词：片名、时间、影院、票价、冲突、选场、移除、复制排片。不要解释功能。
